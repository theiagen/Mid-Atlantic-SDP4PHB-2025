params {
    fastq = 'data/sample.fastq'
}

process {
    executor = 'local'
    withName: test {
        executor = 'test'
    }
    withName: docker {
        executor = 'docker'
        container = 'your-docker-image'
    }
}

docker {
    enabled = true
}